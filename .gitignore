# Gradle
.gradle/
build/
local.properties
*.jks
# Local signing key and credentials (local/CI upload Secrets only, never commit)
keystore/

# IDE
.idea/
.kotlin/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

ref/

# Local runtime assets (books/ is P5-c corpus, synced via syncthing — never commit)
books/
hs_err
